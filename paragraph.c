#include <stdio.h>
int main(){
unsigned int long linecount;
 linecount=0;
 while((ch=getc(stdin))!=EOF)
 {
       if (ch=='\n') {++linecount;} }
 if(line count>0){
  ++linecount;
 }
printf( "%lu \n", linecount );
return 0;
}
